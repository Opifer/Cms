{% extends 'OpiferCmsBundle:Backend:base.html.twig' %}

{% block body %}
    <div class="col-xs-12">
        <div class="page-header">
            <a href="{{ path('opifer_content_directory_new') }}" class="btn btn-link pull-right">
                <i class="material-icons">add</i> {{ 'button.add'|trans|capitalize }}
            </a>
            <h1>Directories</h1>
        </div>
    </div>

    <section class="col-xs-12">
        {{ grid(grid, 'OpiferCmsBundle:Backend/Grid:grid.html.twig') }}
    </section>
{% endblock body %}
