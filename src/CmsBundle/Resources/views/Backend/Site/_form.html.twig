{{ form_start(form, {'style': 'horizontal'}) }}
    {{ form_widget(form) }}

    <div class="form-group form-footer">
        <div class="col-xs-12">
            <input type="submit" value="Save" class="btn btn-primary"/>
            <a href="{{ path('opifer_cms_site_index') }}" class="btn btn-link">{{ 'form.cancel'|trans|capitalize }}</a>
        </div>
    </div>
{{ form_end(form) }}
