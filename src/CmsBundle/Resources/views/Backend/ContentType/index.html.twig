{% extends '@OpiferCms/Backend/base.html.twig' %}

{% block body %}
    {% spaceless %}
        <div class="col-xs-12">
            <div class="page-header">
                <a href="{{ path('opifer_content_contenttype_create') }}" class="btn btn-link pull-right">
                    <i class="material-icons">add</i> {{ 'button.add'|trans|capitalize }}
                </a>
                <h1>{{ 'base.contenttypes'|trans|capitalize }}</h1>
            </div>
        </div>

        {% include 'OpiferCmsBundle:Components:grid.html.twig' with { 'grid': grid } %}
    {% endspaceless %}
{% endblock body %}
