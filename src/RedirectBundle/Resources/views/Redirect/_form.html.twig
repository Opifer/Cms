{{ form_start(form) }}
    {{ form_row(form.origin) }}
    {{ form_row(form.target) }}
    {{ form_row(form.permanent) }}
    {{ form_row(form.requirements) }}

    <div class="form-group form-footer">
        {{ form_widget(form.save) }}
        {{ form_widget(form.saveAndAdd, {'attr' : {'style' : 'margin: 0 5px 0 5px' }}) }}
        <a href="{{ path('opifer_redirect_redirect_index') }}" class="btn btn-link">{{ 'opifer_redirect.form.cancel'|trans }}</a>
    </div>
{{ form_end(form) }}
