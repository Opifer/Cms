{% if block.media|default %}
    <img src="{{ block.media.reference|default('fallback.jpeg')|imagine_filter(block.properties.filter) }}" {{ manage_tags() }}>
    {#{% if block.properties.enlarge|default %}#}
        {#{{ block.properties.enlarge_filter }}#}
    {#{% endif %}#}
{% endif %}
