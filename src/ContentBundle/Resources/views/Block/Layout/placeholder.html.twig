<{{ tag }} {% for key,value in data %}{{ key }}="{{ value }}"{% endfor %}{% if content|length <= 0 %}data-pm-empty="true"{% endif %} {{ manage_tags() }}>
    {{ content|raw }}
</{{ tag }}><!-- end placeholder {{ manage_tags() }} -->
