<div class="container-fluid">
    <div class="row">
        <div class="col-xs-12">
            {{ form_start(form, {'action': path('opifer_content_content_details', {'id': content.id}), 'attr' : {'novalidate': 'novalidate', 'class': 'prevent-enter-submit'}}) }}
                {{ form_widget(form) }}

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">{{ 'button.submit'|trans }}</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
